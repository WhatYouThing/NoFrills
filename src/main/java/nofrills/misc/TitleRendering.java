package nofrills.misc;

public interface TitleRendering {
    void nofrills_mod$setRenderTitle(String title, int stayTicks, int yOffset, float scale, int color);

    boolean nofrills_mod$isRenderingTitle();
}